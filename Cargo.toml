[package]
name = "shielded_transport_pigeon"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Shielded Transport Pigeon, STP, is an encrypted optionally reliable UDP transport layer that does not use TLS."

[dependencies]
